import Controller from './graph-app.controller';

const graphApp = {
    controller: Controller,
    // template : template,
    templateUrl: 'components/graphApp/graph-app.html',
    bindings: {}
};

export default graphApp;